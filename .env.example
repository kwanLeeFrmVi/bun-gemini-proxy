# Gemini Proxy Server Environment Configuration
#
# NOTE: This project uses YAML files for configuration (proxy.yaml and keys.yaml).
# Environment variables are NOT used for configuration.
#
# To configure the proxy:
# 1. Copy proxy.example.yaml to proxy.yaml
# 2. Copy keys.example.yaml to keys.yaml
# 3. Edit the YAML files with your configuration
#
# See proxy.example.yaml and keys.example.yaml for configuration options.
